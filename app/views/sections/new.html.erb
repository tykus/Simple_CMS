<%= link_to "<<Back to list", {:action => 'index'}, :class => 'back-link' %>

<div class="sections new">
  <h2>New Section</h2>

  <%= form_for(:section, :url => {:action => 'create'}) do |f| %>

    <%= render(:partial => "form", :locals => {:f => f}) %>
  
  <div class="form-buttons">
    <%= submit_tag('New Section') %>
  </div>

  <% end %>
</div>